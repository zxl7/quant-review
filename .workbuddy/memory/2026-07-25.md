# 2026-07-25 工作记录

## 检查：线上定时执行 + 财富密码回测（用户要求"检查一下"）

### 方法
用 `gh`（已登录 zxl7）拉取两工作流真实运行记录 + 读 workflow YAML + 读回测脚本 + 查本地/源缓存。沙箱禁直连 curl/gh api 大文件，未能取线上 market_data 全量，但 run 元数据与小 api 正常。

### 线上定时执行（结论：基本可用，1 严重缺口 + 2 失败 + 2 隐患）
1. **【严重】intraday_runtime.yml 07-24（周五）零运行**：gh run list 跨 50 次运行确认 07-24 intraday=0，最后成功 07-23。两工作流均 active（未禁用）→ GitHub cron 漏投/严重延迟。影响：盘中实时页（Watch/异动/快讯）07-24 无更新。
2. **【失败】publish_pages.yml 07-24 两次 intraday 模式失败**：#30069410633(05:25Z)、#30070596525(05:52Z)，失败步骤 "Validate intraday runtime indices"→ `intraday_runtime_indices_incomplete: count=0`。根因链：intraday_runtime 没跑→intraday_runtime.json 空→publish_pages 兜底生成盘中快照也为空→校验失败。触发异常：cron 映射无 intraday 项（workflow_schedule.py 12-20 仅 auction_prefetch/open_fore/eod），疑为早盘 cron 被延迟~4h 误入盘中路径。
3. **【隐患】cron 严重延迟 2-4h**：09:25 竞价快照常延迟到 11:00-13:00 才跑（失盘前意义）；eod 延迟 30min-2hr。GitHub free runner 高峰拥堵，非代码 bug。
4. **【隐患】Node 20 弃用**：日志 `actions/checkout@v4`/`setup-python@v5` target Node 20 被强制 Node 24，未来可能硬失败。

### 财富密码回测（结论：链路完整、源数据完好，强依赖 eod 运行 + 必盈 K 线 API）
1. 源历史 `cache/stock_research_backtest_source.json` 完好：含 15 交易日（06-03~07-06）推送记录，schema v1。早前误判 0 是查错键（实际在 dates 字典下）。
2. `build_stock_research_backtest_payload` 逻辑完整（sync→load rows→enrich performance→realtimeBuy→lifecycle→snapshots）。`_is_backtest_ready_record` 仅排除 status=='missing'。
3. **关键设计**：回测"就绪 rows"仅在 eod 模式才刷历史 K 线——workflow 641 行 `QR_DISABLE_STOCK_RESEARCH_HISTORY_FETCH = mode=='eod'?'0':'1'`。非 eod 构建→histories 空→performance 无法算→backtest_rows=0→线上回测区空/仅待验证池。本地 07-03 缓存 rows=0 与此一致（陈旧+非 eod）。
4. 风险：若 eod 运行延迟/失败 或 必盈 history API（`fetch_stock_history_k`）抖动，回测静默退化为空。

### 建议（未实施，用户仅要求检查）
- P0：给盘中 runtime 加冗余触发（publish_pages eod 顺带校验/补跑 intraday runtime，或午后 watchdog）。
- P1：actions/checkout@v4→@v5、setup-python@v5 更新消除 Node20 弃用。
- P1：回测历史抓取对 eod 失败做容错（保留上一日快照而非清空 rows）。
- P2：评估竞价快照延迟——前端标注"数据截至 HH:MM"，或关键窗口换更高优先级 runner。

### 备注
- 用户偏好：分析内容直接放对话页，不弹 present_files 产物卡片（2026-07-24 立）。本次为检查任务，纯文字结论。
- 未修改任何文件。

## 处理：盘中 runtime 冗余触发（P0，用户"处理"指令，仅改 YML）

### 改动文件
- `.github/workflows/intraday_runtime.yml`：`schedule` cron 由 4 个增至 6 个。
  - 早盘（hour=1 → intraday-runtime-morning 并发组）：`27 1`(新,09:27) / `30 1`(原,09:30) / `40 1`(原,09:40)
  - 午盘（hour=5 → intraday-runtime-afternoon 并发组）：`0 5`(原,13:00) / `3 5`(新,13:03) / `10 5`(原,13:10)
  - 根因：原主 cron 压在 `:00`/`:30` GitHub 高峰分钟，漏投概率最高（07-24 零运行主因）。错峰到 09:27/13:03 规避高峰；并发组按小时归类，错峰分钟不影响 `resolve_intraday_session` 判定。
  - 风险：同窗口多 cron 进入同一并发组会排队串行跑完整会话，CI 分钟消耗上升（已知权衡，按用户 P0 可靠性优先）。

### 顺带核查（仅 YML，未改动）
- **Node 20 弃用**：`publish_pages.yml` 已用 `actions/setup-node@v4` + `node-version: "22"` + `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"` → 弃用已解决，无需改。
- **回测失败保留旧快照**：编排层已满足——fetch/校验失败阻断最终 push，gh-pages 保留上一版。深层（仅 backtest 子抓取失败但主 fetch 成功时静默缺数）属 Python 逻辑，超出 YML 范围，未处理。

### 待办（需改 Python，未做）
- 回测子抓取容错：`daily_review/` 内 backtest 刷新逻辑需 try/except 保留旧快照；`resolve_intraday_session` 若按精确 cron 串（非小时）归类，新增错峰 cron 可能被判 `once`，需复核。
