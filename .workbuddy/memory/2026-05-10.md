# 2026-05-10

## 修复短线提醒（learningNotes）为空的 bug

1. **`notes_loader.py` NameError**: 删除了第 154-159 行引用函数内部变量 `entries` 的 debug print，导致 `build_learning_notes` 整体崩溃
2. **`modules_v2/plan/learning_notes.py` 路径错误**: `_workspace_root()` 用了 `parents[2]` 指向 `daily_review/` 而非项目根，改为 `parents[3]`。这导致 pipeline 重建时找不到 `心法.md` 和 `cache/` 目录
3. **`render_html.py`**: 上一轮已修复（`setdefault` → `if not ln.get("tips")`），本次确认仍有效

全量 fetch 验证通过：tips/quotes 正常从"心法标准调用池"按情绪阶段抽取，所有字段完整。

## 修复实时盯盘快照数据缺失问题

**根因分析**：cli.py 中快照构建代码在 `run_rebuild()` 之前执行（第584行），此时 market_data 中 volume/plateRankTop10 等字段尚未被 pipeline 填充。

修复内容：
1. **快照构建移到 run_rebuild 之后**，并从 rebuild 后的缓存文件重新读取完整数据
2. **lianban_count=0 bug**：`build_mood_inputs()` 不设置 `lianban_count`，改为从 `lb_2+lb_3+lb_4p+lb_5p` 计算（与 build_v3_sentiment_inputs 一致）
3. **amount="" bug**：从 rebuild 后的 `market_data["volume"]["total"]` 读取（之前在 rebuild 前读的空值）
4. **concepts=[] bug**：数据源改用 `plateRankTop10 or conceptFundFlowTop`（原只用 plateRankTop10）
5. **每次 fetch 删除当日 slices**：cli.py 第204-206行每次 fetch 都 `unlink` 了 intraday_slices 文件，导致连续运行只保留最后一条。删除该代码，跨日清理改由 `_purge_previous_day_slices` 处理
6. **添加跨日清理**：在 cli.py fetch 路径中加入 `_purge_previous_day_slices` 和 `_purge_previous_day_published` 调用
7. **修复 `_norm_index_chg` 未定义**：cli.py 第491行引用了不存在的函数，替换为直接 dict comprehension

## 为 cli.py 添加执行节点日志

在 `run_fetch_and_rebuild`、`run_rebuild`、`run_intraday_snapshot`、`run_partial`、`main` 各关键节点添加 `_log()` 时间戳日志输出，覆盖：交易日确认、数据池获取、题材缓存、指数K线、高度趋势、实时行情、缓存写入、pipeline 执行、快照注入、HTML 渲染等阶段。入口处打印模式标识行（▶ [fetch/rebuild/partial/intraday/full]）。

## 修复趋势颜色 span 被旧 CSS 覆盖导致纵向排列

模板中存在两个 `.trend-v` 定义：第5773行是旧的 sparkline 容器样式（`display: flex`），第5997行是新的趋势颜色 span（需要 inline）。CSS 同名覆盖导致趋势值变成 flex 纵向排列。将旧样式重命名为 `.trend-spark-row` 消除冲突。
