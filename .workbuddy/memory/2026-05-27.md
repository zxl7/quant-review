# 2026-05-27 工作日志

## 修复
- `context.py` 缺少 `get()` 方法导致 CI 构建失败：新增了 `Context.get()` 方法但未 commit/push。已在 `9af82a4` 提交中修复。

## 新增功能
- **17:00 预获取"今日题材"数据**：今日题材 (TomorrowPicksPage) 之前完全依赖前端运行时调东方财富 API，经常 CORS/网络失败。新增：
  - `tools/fetch_eastmoney_tomorrow.py`：Python 预获取脚本，在 CI 非盘中模式调用 `eastmoney_theme.save_tomorrow_snapshot()`，将东方财富 API 数据拍平为前端 `TomorrowTheme[]` 格式，写入 `web/public/eastmoney_tomorrow.json`
  - `inject_data.py`：注入 `window.__EASTMONEY_TOMORROW_DATA__`（含 themes + stocksByTheme），同步到 dist/ 目录
  - `useTomorrowPicks.ts`：3 层回退——Tier 1 预注入数据 → Tier 2 运行时 API 调用。成份股也优先使用预注入数据
  - CI workflow：Pre-fetch 步骤增加 eastmoney 获取；site 发布 + site_prev 同步增加 `eastmoney_tomorrow.json`
  - 提交：`58b45c2 feat: 17:00预获取东方财富今日题材数据并注入前端`
