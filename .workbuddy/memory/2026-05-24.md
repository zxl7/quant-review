## 2026-05-24

### 新增"明日"Tab（东方财富 API 对接版）
- **数据源**：东方财富 `emcfgdata.eastmoney.com`
  - `getFryTomorrowList` → 策略池列表（主题方向/涨停数/历年累计涨幅）
  - `getStockList` → 成分股明细（含入选理由/连板标签）
  - CORS 有限制 → 浏览器端可能卡住 → 三层兜底：`__TOMORROW_PICKS__`（CI注入） → API → `tomorrow_picks.json`
- **前端**：`web/src/components/tomorrow/`
  - `TomorrowPicksPage.vue`：左侧日期选择器+策略卡片，右侧成分股详情
  - `useTomorrowPicks.ts`：API 调用 + 本地 fallback + 超时处理
  - 日期选择器：dropdown 切换日期过滤
  - Loading 骨架：shimmer 动画占位符
- **CI 预取**：`tools/fetch_tomorrow_picks.py`，CI 时运行产生 `web/dist/tomorrow_picks.json`
- **CSS 修复**：`template.css` 添加 `body[data-page="tomorrow"] .card[data-page="tomorrow"]` 规则（页面过滤机制）
- **App.vue**：reviewTabs `{ id: 'tomorrow', name: '明日' }`
- **解耦**：完全不依赖 marketData，纯 API 驱动
