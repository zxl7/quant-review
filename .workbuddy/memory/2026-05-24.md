## 2026-05-24

### 新增"明日"Tab（东方财富 API 对接）
- 数据源：`emcfgdata.eastmoney.com` → `getFryTomorrowList` + `getStockList`
- 三层兜底：`__TOMORROW_PICKS__`（CI注入） → API → `tomorrow_picks.json`
- 设计：统一 design token，日期选择器，shimmer 骨架屏
- 文件：`web/src/components/tomorrow/TomorrowPicksPage.vue` + `useTomorrowPicks.ts`

### 龙虎榜组件重写（替换 iframe）
- 从 iframe 嵌套 → Vue3 组件，数据源：`page1.tdx.com.cn:7615/TQLEX?Entry=CWServ.cfg_fx_yzlhb`
- 买入前N席 + 卖出前N席双栏布局，按股票卡片分组
- 去重合并（同 gpdm+yzmc+yyb 取最大值）、null 值正确保留
- 三层兜底：`__DRAGON_TIGER_DATA__` → 本地 JSON → API
- 文件：`web/src/components/dragon-tiger/DragonTigerPage.vue` + `useDragonTiger.ts`

### CI 修复（线上实时兼容）
- `inject_data.py`：同时注入 `__MARKET_DATA__` + `__DRAGON_TIGER_DATA__` + `__TOMORROW_PICKS__`
- `.github/workflows/publish_pages.yml`：
  - 新增 Pre-fetch 步骤（fetch_dragon_tiger.py + fetch_tomorrow_picks.py）
  - Prepare Pages 步骤复制 JSON 文件到 site 目录

### 其他修复
- `template.css`：+`body[data-page="tomorrow"] .card[data-page="tomorrow"]` 规则
- DragonTigerPage.vue：HTTP→HTTPS（iframe 链接）
- `.gitignore`：+ `.playwright-cli/`、`web/dist/*.json`
